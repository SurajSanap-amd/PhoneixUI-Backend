package com.TPB.team_portal_backend.repository;

import org.springframework.stereotype.Repository;


@Repository
public interface SubmitIdeaRepository extends IdeaRepository {
    // Add idea submission–specific queries if needed later
}
